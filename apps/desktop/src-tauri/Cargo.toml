[package]
name = "treble-tracker-desktop"
version = "0.1.0"
edition = "2021"
description = "Treble Tracker desktop shell"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true
