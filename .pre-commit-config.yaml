# Enforces CLAUDE.md §2 tooling on every commit. CI runs the same checks via `make check`.
repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: .venv/bin/ruff check --fix
        language: system
        types: [python]
      - id: ruff-format
        name: ruff format
        entry: .venv/bin/ruff format
        language: system
        types: [python]
      - id: mypy
        name: mypy --strict
        entry: .venv/bin/mypy treble
        language: system
        pass_filenames: false
        always_run: true
      - id: tests
        name: test suite + coverage floor
        entry: .venv/bin/pytest -q
        language: system
        pass_filenames: false
        always_run: true
      - id: import-linter
        name: import-linter (I7)
        entry: .venv/bin/lint-imports
        language: system
        pass_filenames: false
        always_run: true
